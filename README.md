# hello.json

const http = require('http'); //This builds up a web server , then you must create server
const server= http.create server(function(req,res));//req is the request object ,res is the reponse object

server.listen(0010, function(){ //port number is 0010 
console.log ('Loading message...')
